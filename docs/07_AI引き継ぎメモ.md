# AI（Claude）への引き継ぎメモ

> このファイルは Claude が次回セッションで素早くシステムを理解するために作成された資料です。
> 作業開始時にこのファイルを読んでください。

---

## システムの一言要約
クレー射撃大会の成績管理Webアプリ。管理者が選手管理・点数入力を行い、選手・観客がQRコードで成績を閲覧する。Next.js + Neon PostgreSQL + Vercel構成。

---

## まず読むべきファイル（優先順）
1. `docs/01_システム概要.md` — 全体構成・URL・ファイル構成
2. `docs/03_DBスキーマ.md` — テーブル構造
3. `docs/04_ビジネスルール.md` — 仕様詳細
4. `lib/types.ts` — TypeScript型定義（最重要）
5. `lib/colors.ts` — カラー定数（C.gold, C.text等）

---

## 重要な設計上の注意点

### 1. PostgreSQL数値型→文字列問題
```typescript
// ❌ ダメ（文字列にtoFixedはない）
r.average?.toFixed(2)

// ✅ 正しい
Number(r.average).toFixed(2)
```
`v_results` ビューのNUMERIC集計値はJavaScriptで文字列として届く。

### 2. member_code が紐付けキー
- `members` テーブルと `scores` テーブルは `member_code` で紐付け（外部キーなし）
- `member_code` が NULL の選手は点数登録不可（UIで警告あり）

### 3. Next.js App Router の params
```typescript
// params は Promise<> でラップされている
const { id } = await params;  // ← await 必須
```

### 4. Windowsローカルビルドについて
- `next build` はEPERMエラーで失敗する場合がある（Windows固有）
- TypeScriptチェック: `npx tsc --noEmit --incremental false`（エラーなしなら問題なし）
- 実際のビルドはVercel（Linux）が行うため、pushすれば自動で確認できる

### 5. コンポーネント構成（よく変更される箇所）
| ファイル | 変更頻度 | 内容 |
|---------|---------|------|
| `components/admin/MembersTab.tsx` | 高 | 選手管理の全機能（インライン編集・DnD組替） |
| `components/admin/ScoresTab.tsx` | 高 | 点数入力（テンキーポップアップ） |
| `components/viewer/ViewerPage.tsx` | 中 | 閲覧者向け成績表示 |
| `components/admin/SettingsTab.tsx` | 中 | 大会設定・QRコード・危険ゾーン |
| `components/admin/ApplySettingsTab.tsx` | 中 | 申込設定（募集人数・日時・スケジュール） |
| `lib/colors.ts` | 低 | カラーテーマ変更時のみ |
| `lib/kanji-normalize.ts` | 低 | 旧字体→新字体変換テーブル（選手検索で使用） |
| `lib/prefectures.ts` | 低 | 都道府県リスト（47+芸文+学連）。MembersTab・PlayersPageで共用 |
| `app/admin/players/page.tsx` | 中 | 選手マスター管理画面 |
| `app/api/players/route.ts` | 中 | 選手マスターAPI（GET/POST） |
| `app/api/players/[code]/route.ts` | 中 | 選手マスターAPI（PUT/PATCH/DELETE） |

---

## よくある改善リクエストのパターン

### UIテキスト変更
→ 該当コンポーネントを検索して直接編集

### 画面追加
→ `app/` にページ追加、`app/api/` にAPIルート追加

### DB変更
→ `db/schema.sql` を更新、Neon SQLエディタで手動実行、`lib/types.ts` も更新

### エラーが特定の画面で出る
→ まず `lib/types.ts` でデータ型を確認、次にAPIレスポンスの型を確認

---

## デプロイ方法
```bash
# TypeScript確認
npx tsc --noEmit --incremental false

# コミット＆プッシュ（Vercelが自動デプロイ）
git add <ファイル>
git commit -m "変更内容"
git push origin main
```

---

## Vercel環境変数（認証システム用）

管理者認証を動作させるには、Vercel ダッシュボードで以下の環境変数を設定する必要があります。

| 変数名 | 値 | 説明 |
|--------|-----|------|
| `NEXTAUTH_SECRET` | ランダム文字列32文字以上 | JWTの署名鍵。`openssl rand -base64 32` で生成 |
| `NEXTAUTH_URL` | `https://clay-shooting.vercel.app` | 本番URL |
| `GOOGLE_CLIENT_ID` | Google Cloud Consoleで取得 | Google OAuth クライアントID |
| `GOOGLE_CLIENT_SECRET` | Google Cloud Consoleで取得 | Google OAuth クライアントシークレット |
| `SYSTEM_ADMIN_EMAILS` | `matsuo@repros.co.jp` | Googleログイン許可メール（カンマ区切りで複数可） |

### Google Cloud Console の OAuth設定手順
1. https://console.cloud.google.com/ にアクセス
2. プロジェクト選択 → 「APIとサービス」→「認証情報」
3. 「OAuthクライアントIDを作成」→ アプリケーションの種類：**ウェブアプリケーション**
4. 承認済みのリダイレクトURI に追加：
   - `https://clay-shooting.vercel.app/api/auth/callback/google`
5. クライアントID・クライアントシークレットをコピーしてVercelに設定

---

## 認証情報

- 管理者ログインは `/admin/login` から（v3.78でトップページの隠しボタンは廃止）
- システム管理者: Google OAuth（`SYSTEM_ADMIN_EMAILS` で許可制）
- 運営管理者: 会員番号 + bcryptパスワード（`tournament_admins` テーブル）
- データリセットは設定タブ「危険ゾーン」から（ログイン中の管理者本人確認に基づく）

> DB接続文字列は `C:/projects/clay-shooting/開発メモ.txt` を参照

---

## 現在の本番URL
- **トップ**: https://clay-shooting.vercel.app/
- **管理者**: https://clay-shooting.vercel.app/admin
- **閲覧者**: https://clay-shooting.vercel.app/viewer

---

## 最新コミット時点の主要機能一覧（2026年5月時点、v3.96.2）
- ✅ Tailwind 漸進移行: ApplySettingsTab.tsx を utility class 化（v3.96.2、2026-05-20）
  - `style={{}}` 32件 + `React.CSSProperties` 2個（inputStyle / labelStyle）+ JSX 定数 3個（requiredMark / dpDateTimeInput / dpTimeInput） をすべて Tailwind utility class / className に置換
  - 特殊カラー（`${C.green}22` / `${C.green}33` / `#1a1a2e`）は arbitrary value で完全保持
  - 動的カラー切替（squadPublishedAt / squadUrlCopied / savingApply / squadSaving）は template literal で条件分岐
  - `lib/colors.ts` の `C` import を削除
  - VRT (`/admin/1?tab=apply-settings`、v3.95 baseline) で pixel 一致を CI 確認
- ✅ Tailwind 漸進移行: InspectionTab.tsx を utility class 化（v3.96.1、2026-05-20）
  - `style={{}}` 19件 + 名前付き `React.CSSProperties` 9個 を Tailwind utility class に置換
  - pixel 完全一致のため `text-[14px]` 等の arbitrary value を使用（`text-sm` の line-height 注入回避）
  - カラーは `@theme` 経由（`bg-surface` / `text-gold` / `border-border` 等）
  - `lib/colors.ts` の `C` import を削除
  - VRT (`/admin/1?tab=inspection`、v3.96 baseline) で pixel 一致を CI 確認
  - 本番ランタイム影響ゼロ（DOM 構造・state ロジック・API 呼び出しは無変更）
  - 次対象候補: `ApplySettingsTab.tsx`(32) / `ScoresTab.tsx`(52) / `MembersTab.tsx`(120)
- ✅ VRT 拡張 Phase 3 第3弾: inspection / report / logs 追加（v3.96、2026-05-19）
  - 追加画面: `/admin/1?tab=inspection` / `?tab=report` / `/admin/logs?action=score_save`
  - Phase 3 が**実質完了状態**: 合計 11 画面（v3.94 の 4 + v3.95 の 4 + v3.96 の 3）
  - fixture 完全追記方式: 既存大会 ID=1 周りは無変更、tournament_reports / report_incentives / operation_logs を新規 INSERT
  - `/admin/logs` は URL filter `?action=score_save` で setup project の動的 login record を除外（戦略D）
  - 日時セル（`tbody td:nth-child(1)`）は locator mask（戦略C）
  - 残: `/admin/logs` 全件表示は別フェーズ（setup login record 吸収方法要）、システム管理者専用画面（system-admin 用 CredentialsProvider 追加要）
- ✅ VRT 拡張 Phase 3 拡大: 4 画面追加（v3.95、2026-05-19）
  - 追加画面: `/admin/1?tab=registrations` / `?tab=settings` / `?tab=apply-settings` / `/admin/players`
  - fixture 拡張: `player_master` に 6 名（900001〜900006、members fixture と整合）追加 → `/admin/players` VRT 所属 7 名で安定 baseline
  - 動的要素なし: `info_saved_at` / `apply_saved_at` / `squad_published_at` は fixture で NULL → 「最終保存」「公開中」表示は非表示
  - QRコードは URL ベースで決定的（CI では `origin = http://localhost:3000` 固定）
  - 機構は v3.94 と同一: setup project + chromium-auth project + e2e-auth job、playwright.config.ts / e2e.yml 変更なし
  - 残: inspection / report タブ（fixture 拡張必要）、`/admin/logs`（動的データ、mask 戦略確立要）、システム管理者専用画面（admins / associations / shooting-ranges）
- ✅ VRT 拡張 Phase 3: 認証要画面（v3.94、2026-05-19）
  - 対象 4 画面: `/admin`（大会一覧）/ `/admin/1?tab=members` / `?tab=scores` / `?tab=results`
  - `CredentialsProvider('tournament-admin')` 経由でログイン、Google OAuth mock 不要
  - vrt-baseline branch の `tournament_admins` に固定アカウント (member_code=999999, パスワード `vrt-admin-pass-2099`, bcryptjs round 10) を fixture コミット
  - `e2e/auth.setup.ts`（setup project）が storageState を `e2e/.auth/admin.json` に保存 → Phase 3 spec が再利用
  - `playwright.config.ts` に `setup` project + `chromium-auth` project（dependencies: ['setup']）を追加
  - CI: `.github/workflows/e2e.yml` に `e2e-auth` ジョブ追加（`needs: e2e-data` で直列化、Phase 1 grep-invert を `"Phase [23]"` に拡張）
  - 残: registrations / settings / apply-settings / inspection / report / `/admin/players` / `/admin/logs`（次フェーズ候補）
- ✅ VRT 拡張 Phase 2: data 依存ページ（v3.93、2026-05-19）
  - 対象 3 画面: `/`（大会一覧通常表示）/ `/viewer/1`（成績表示）/ `/tournaments/1/apply`（申込ページ正常系）
  - Neon `vrt-baseline` branch（production からの copy-on-write）に固定 fixture を投入して使用
  - `db/seed/vrt-fixtures.sql`（大会1件 + 6名 + スコア + 申込2件、2099 年日付固定）
  - `scripts/vrt-seed.js`（`VRT_DATABASE_URL` + `VRT_SEED_FORCE=1` 必須）
  - CI: `.github/workflows/e2e.yml` に `e2e-data` ジョブ追加（`needs: e2e` で直列化）
  - GitHub secret `VRT_DATABASE_URL` 登録済
  - **Phase 1 と Phase 2 は別 job** — Phase 1 のダミー DB 前提 spec が Phase 2 接続で壊れないよう `--grep-invert "Phase 2: data 依存"` で除外
  - migration 追加時は vrt-baseline branch にも反映必要（CLAUDE.md 参照）
- ✅ Tailwind 化 D+ 段階導入の初期セットアップ（v3.92、2026-05-19）
  - `app/globals.css` の `@theme` を `lib/colors.ts` の全 11 値（bg / surface / surface-2 / border / gold / gold-dark / blue-2 / red / green / text / muted / input-bg）に統一
  - 既存の未使用 `--gold-light` を削除、`--color-gold` を `#d4a017` → `#e8a020`（`C.gold` と一致）
  - `react/forbid-dom-props` を `style` 属性に `warn` で追加（新規追加時のガード、既存 1689 件は warn）
  - `CLAUDE.md` に「スタイリング方針（v3.92〜、Tailwind 化 D+ 段階導入）」セクション新設
  - 移行運用: 新規は Tailwind utility、既存は触ったファイル単位で段階的に utility 化（D+ 機会主義）
  - 本番ランタイム影響ゼロ（既存 `style={{}}` ピクセルは変わらず、削除した CSS 変数も未参照）
- ✅ VRT 拡張 Phase 1（v3.91、2026-05-19）
  - 確定エラー状態の 3 画面を VRT 対象に追加（追加依存ゼロ）
    - `/admin/register`（token なし）→ 「招待リンクが無効です」
    - `/admin/reset-password/[無効token]` → CI ダミー DB で確定エラー
    - `/`（大会なし状態）→ 「大会が登録されていません」
  - baseline は workflow_dispatch + `update_snapshots=true` で生成（既存運用と同じ）
  - 残: data 依存ページ（大会一覧通常表示・閲覧者・申込正常系）・認証要ページは staging seed / login fixture 前提
- ✅ CSP 強制モード化（v3.90、2026-05-18、D-1b Phase 2 step B 完了）
  - レスポンスヘッダを `Content-Security-Policy-Report-Only` → `Content-Security-Policy` に rename
  - 自動巡回（Claude in Chrome）で 12 シナリオ違反 0 件確認後の移行
  - `'unsafe-inline'` `'unsafe-eval'` 撤廃、`script-src` は nonce + strict-dynamic で限定
  - Mozilla Observatory: Grade A → A+ 圏到達期待
  - ロールバック: middleware の 1 行を Report-Only に戻すだけ
- ✅ CSP nonce + strict-dynamic 化（v3.89、2026-05-18、Report-Only モード並走）
  - D-1b Phase 2 step A: per-request nonce 生成 + `strict-dynamic`、`'unsafe-inline'` `'unsafe-eval'` を script-src から撤廃
  - `middleware.ts` matcher を全ページに拡張、Next.js 公式パターンで実装
  - `style-src` は `'unsafe-inline'` 維持（既存 `style={{}}` 1686 件のため）
  - Report-Only モード並走で本番違反監視 → 別 PR (step B) で本ヘッダ化予定
  - 期待効果: Mozilla Observatory Grade A → A+ 圏（step B 完了時）
- ✅ ビジュアル回帰テスト (VRT) 基盤導入（v3.88、2026-05-14）
  - Playwright `toHaveScreenshot()` 採用、追加依存ゼロ
  - 対象 4 画面: 管理者ログイン / パスワード再発行 / FAQ / お問合せ（DB/認証不要の静的ページ）
  - `<footer>` mask（APP_VERSION 表記の差分回避）
  - GitHub Actions workflow_dispatch に `update_snapshots` 入力追加（true で baseline 自動 commit）
  - **baseline 生成はマージ後 workflow_dispatch で実行必須**（初回 PR 時点では baseline 未生成のため visual テスト失敗）
  - 戦略的意義: Tailwind 化・CSP 強化・dep upgrade の安全網
- ✅ CVE T2 解消: nodemailer 7 → 8 major upgrade（v3.87.3、2026-05-14）
  - GHSA-c7w3-x93f-qmm8 / GHSA-vvjj-xcjg-gr5g（SMTP コマンドインジェクション 2件）解消
  - nodemailer 8.0.0 の唯一の breaking change（`NoAuth` → `ENOAUTH`）は本プロジェクト未使用
  - next-auth は Email Provider 未使用のため peer dep warning のみ実害なし
  - **npm audit: 2 件 → 0 件 達成** ✅
  - 残: D-1b CSP 強制モード化、P-1 リージョン整合のみ
- ✅ next 16.2.3 → 16.2.6 CVE 群一括解消（v3.87.2、2026-05-14）
  - HIGH 7件含む 13 件の CVE を一括解消（SSRF / Middleware バイパス / DoS / XSS / cache poisoning）
  - `next` `eslint-config-next` ともに 16.2.6 へ minor upgrade（breaking なし）
  - `npm audit` 残: nodemailer 関連のみ（D-2 で別対応）
- ✅ CVE T3 解消: vite/esbuild override（v3.87.1、2026-05-14）
  - `package.json` の `overrides` に `esbuild: ^0.25.0` と `vite: ^6.4.2` 追加
  - esbuild GHSA-67mh-4wv8-2f99 (Med) と vite CVE-2026-39365 (Med) 解消（両方 dev only）
  - `vitepress@1.6.4` は vite 6 互換、`npm install` peer dep error なし
  - 残: 新規 next 16.2.3→16.2.6 CVE 群（HIGH 多数）、nodemailer 7→8 (T2/D-2) は別 PR
- ✅ registrations 複合インデックス追加（v3.87、2026-05-13）
  - migration 027: `idx_registrations_tournament_member ON registrations (tournament_id, member_code)`
  - 申込重複チェック・会員番号同期等のホットパスを高速化
  - ⚠️ **migration 027 は GitHub Actions で自動適用**（staging push → main push）
- ✅ reset-password ページのレイアウト崩れ修正（v3.86.1、2026-05-13）
  - ルート `<div>` の `flex-direction: row` のせいでフォームと Footer が横並びに圧縮されていたバグ
  - `forgot-password` (v3.84) と同じ flex-column パターンに修正
- ✅ Resend 残骸の完全削除（v3.86、2026-05-12）
  - `package.json` から `resend` 依存削除（dead dependency）
  - Vercel PROD env `RESEND_API_KEY` / `EMAIL_FROM=onboarding@resend.dev` 削除
  - Resend ダッシュボード API キー 3 件 Revoke 済
  - `EMAIL_FROM` 削除により `lib/email.ts` は `GMAIL_USER` にフォールバック（実質変更なし）
- ✅ CSP `Report-Only` 段階導入（v3.85、2026-05-12）
  - `next.config.ts` の `SECURITY_HEADERS` に `Content-Security-Policy-Report-Only` 追加
  - Next.js が要求する `'unsafe-inline'` / `'unsafe-eval'` は一旦許可（次フェーズで nonce 化検討）
  - 違反レポート受信: `POST /api/csp-report` → Vercel ログに warn 出力（DB 保存なし）
  - Mozilla Observatory A → A+ 圏狙い、現状挙動は完全に維持
- ✅ パスワードリセットのレート制限 + 明示的エラー化（v3.84、2026-05-11）
  - 同一 IP から **1 時間 5 回** までに制限（スライディングウィンドウ）、超過時 HTTP 429 + Retry-After
  - 未登録メアド送信時 HTTP 404 で「登録されたメールアドレスは存在しません。」明示表示
  - 429 受信時 UI: 動的カウントダウン付きエラーメッセージ（迷惑メール確認の案内 + システム管理者連絡先）
  - エラー表示は送信ボタン直上にインライン（旧 ErrorModal は廃止）
  - DB: `password_reset_attempts (id, ip, created_at)` 追加 + `(ip, created_at)` インデックス
  - ⚠️ **migration 026 はNeonで自動適用される**（staging push → main push）
- ✅ CVE T1 解消: postcss 8.5.14 override 再挑戦（v3.83.7、2026-05-11）
  - v3.83.1 で npm v10/v11 lock file 互換性問題で revert したものを再挑戦
  - CI workflow 5 ファイルに `npm install -g npm@11` ステップを追加
  - `package.json` に `overrides: { "postcss": "^8.5.14" }` を追加
  - 全 postcss インスタンスが 8.5.14 に統一、GHSA-qx2v-qp2m-jg93 (Med) 解消
  - 残: nodemailer 7→8 (T2)、vite/esbuild (T3 dev only) は別 PR
- ✅ 「氏名（ 🚩審判）」ヘッダ統一 + 申込手動追加の並列化（v3.83.6、2026-05-08）
  - 全画面（射順発表・成績確認・点数登録・閲覧者・選手管理（通常+組替）モード）でヘッダを「氏名（ 🚩審判）」に統一
  - フラグマークを `⚑` (U+2691) → `🚩` (U+1F6A9) に全画面統一
  - 選手管理タブ（通常モード）の独立した「審判」列を削除し、氏名セル内に🚩を表示（空席行 colSpan を 8→7 に調整）
  - 申込手動追加: `handleManualSave` の直列 `for await` ループを `Promise.all` で並列化、約 6 倍速（6行 8秒→1.4秒）
- ✅ 射順発表の列幅統一 + 成績一覧の射順安定ソート + viewer 自動リフレッシュ（v3.83.5、2026-05-08）
  - 射順発表ページ: 各組ごとの `<table>` を廃止 → 1テーブル + 組ごとの `<thead>/<tbody>` 構造（Fragment 化）。🚩 の有無で組ごとに列幅がずれる問題を解消
  - results API: ORDER BY の最終キー前に `COALESCE(group1, 999), COALESCE(group2, 999), COALESCE(position, 999)` を追加。点数未登録時は射順順で安定
  - ViewerPage: `fetchResults(silent=true)` で 30秒ごとに自動再 fetch（ちらつき防止のため loading/error は触らない）
- ✅ 射順発表ページ: スマホ向けレイアウト改善 + 30秒自動リフレッシュ（v3.83.4、2026-05-08）
  - 組ごとに小テーブル分割（ヘッダ行: N組 / 所属 / 氏名 / クラス）
  - 「審判」列を削除し、氏名の後ろに 🚩 を表示（v3.79 点数登録と統一）
  - 「所属協会」見出しを「所属」に短縮、氏名は `whiteSpace: nowrap` + 横スクロール許容
  - `setInterval(30_000)` で自動再 fetch、ページを離れたら停止
  - セクションヘッダーに「↺ 最新表示」手動ボタンを追加
- ✅ 組替モード: 前詰めを組またぎに拡張（v3.83.3、2026-05-08）
  - v3.83.2 は同じ組の中だけで詰めていたため、移動結果として **途中の組** に空きが残るバグがあった
  - `compactAllSlots()` に書き換え、全 day 通しで (group, position) 順に前詰め
  - 空きは（同 day 内の）最終組の末尾だけに集約される
  - 空席指定 (isUnused=true) は位置固定（飛ばし、v3.79 仕様維持）
- ✅ 組替モード: 移動後の空きスロットを自動詰め（v3.83.2、2026-05-08）
  - `confirmMove()` 末尾で `compactWithinGroups()` を適用（v3.83.3 で `compactAllSlots()` に置換）
  - v3.79 リスト挿入カスケード単独だと、移動先や経路に既存空きがあると元位置に空きが残るバグを修正
- ✅ CVE T1 解消: next High DoS（v3.83.1、2026-05-08）
  - next 16.2.1 → 16.2.3（GHSA-q4gf-8mx6-v5v3、CVSS 7.5）
  - eslint-config-next も同期
  - postcss XSS（Med）は `overrides` 試行も npm v11/v10 lock file 互換性問題で別PR保留
- ✅ HTTP セキュリティヘッダ強化（v3.83、2026-05-08）
  - `next.config.ts` の `headers()` で全ページに defense-in-depth ヘッダ追加
  - X-Content-Type-Options / X-Frame-Options / Referrer-Policy / Permissions-Policy / COOP / CORP
  - `poweredByHeader: false` で `X-Powered-By: Next.js` 非表示
  - Mozilla Observatory: Grade C/50 → A 圏（CSP は次段階で別 PR）
  - HSTS は Vercel 側で既に付与済
- ✅ 申込管理タブ: 会員番号インライン編集を廃止（v3.82、2026-05-06）
  - 編集モードで会員番号セルは表示専用（`<input>` を出さない）
  - 会員番号の修正は選手マスター（/admin/players）のみ
  - 申込済みの会員番号を変えたい場合は「キャンセル → 削除 → 手動再追加」
  - 副次効果: 編集モード中に手動追加した行が空欄に見える表示バグも解消
  - PATCH `/api/tournaments/[id]/registrations/[regId]` の `body.member_code` 分岐は削除済み
- ✅ 選手マスター: 氏名の自動上書き禁止 / 申込時に審判フラグ反映（v3.81、2026-05-05）
  - `player_master.name` は申込・手動追加経由では更新されない（新規 INSERT 時のみ設定可）
  - 異体字消失防止（過去事例: 「濵﨑 航平」→「濱崎」）
  - 申込時 (`apply/submit`) で `is_judge` を反映、`change_history` に「審判 無→有」等を記録
  - **同日に xlsx ベース一括復旧 SQL 実行済み**: T欠落51名・S欠落5名・氏名2名・所属12名・新規179名
- ✅ 射順URLコピー & キャンセル期限切れ時のリンク非表示（v3.80、2026-05-05）
  - 申込設定タブに「🔗 射順URLをコピー」ボタン追加
    - 公開中: ワンクリックで `/tournaments/{id}/apply#squad` をクリップボードコピー、押下後 `✓ コピー済` に2秒変化
    - 非公開時: `射順URL: 準備中` テキストのみ表示
  - 申込ページの「申込キャンセルの方はこちら」リンク
    - `cancel_end_at + 5分` を超えていたら非表示（バックエンド検証と一致）
- ✅ 組替モード仕様変更・空席設定機能・ログイン遷移修正など（v3.79、2026-05-02）
  - 組替モード: DnD 廃止 → ⋮⋮ アイコンクリックで移動モーダル（スマホ崩れ対策）
  - 空席設定（新規）: `tournaments.unused_slots JSONB`、組替モードに「空席設定」ボタン
  - ランダム組替: プレビュー状態を持つ（保存/キャンセルで確定/破棄、プレビュー中は手動編集ロック）
  - 点数登録: 審判フラグ ⚑ を氏名の後ろに表示
  - システム管理者ログイン: 常に `/admin` に遷移（Google OAuth、callbackUrl 無視）
  - 申込→選手管理 移行: ローディングオーバーレイ表示
  - ⚠️ **migration 025 (`unused_slots` カラム追加) はNeonで自動適用される**
- ✅ lib/email.ts を環境別 SMTP 対応に変更（v3.78.9、2026-05-01）
  - `EMAIL_HOST` 設定で任意SMTP（staging Mailtrap 等）、未設定なら Gmail（本番後方互換）
  - 新規任意環境変数: `EMAIL_HOST`, `EMAIL_PORT`, `EMAIL_USER`, `EMAIL_PASS`, `EMAIL_FROM`
  - 既存 `GMAIL_USER` / `GMAIL_APP_PASSWORD` も引き続き有効
- ✅ トラブルシューティング集 & ESLint 警告 12件削減（v3.78.8、2026-04-30）
  - `docs/09_トラブルシューティング.md` 新設（Docker/CI/Neon/Vercel/Mailtrap/Playwright/Git）
  - 安全な ESLint 警告（unused-vars 11 + html-link 1）を修正、43→31 件
  - 残存はすべて `react-hooks/*` 系（要丁寧な修正、別PRで段階的対応）
- ✅ Docker ローカル開発環境を追加（v3.78.7、2026-04-30）
  - `Dockerfile`、`docker-compose.yml`、`.dockerignore`、`.env.example` 新設
  - `package.json` に `dev:docker`、`migrate` スクリプト追加
  - 3環境運用（local / staging / production）の "local" 部分を提供
- ✅ Playwright E2E テスト基盤を導入（v3.78.6、2026-04-30）
  - playwright.config.ts + e2e/ 6ファイル + .github/workflows/e2e.yml
  - スモークテスト（DB不要）は CI で自動実行、フローテストは fixme で skip
  - フルテストは GitHub Actions の `workflow_dispatch` で staging URL 指定して手動実行
- ✅ CI / Migration 自動実行 / ESLint / 3環境ワークフロー導入（v3.78.5、2026-04-30）
  - GitHub Actions: ci.yml（tsc + lint + build）, migrate-staging.yml, migrate-production.yml
  - `_migrations` テーブル（migration 024）+ `scripts/run-migrations.js`
  - `staging` ブランチ作成 + main/staging ブランチ保護（直接push禁止）
  - ESLint 9 flat config（eslint-config-next）導入、既存コードの hook ルールは一旦 warn
  - **Claude は今後 feature/* で作業 → PR → staging → PR → main の順で進める**
- ✅ CLAUDE.md にセッション起動プロトコル追加（v3.78.3、2026-04-30）
  - キーワード「起動」または「現状把握」で現状サマリ自動報告
  - version → 引き継ぎメモ → 直近3バージョン → 残タスク → 次候補 の順で確認
- ✅ docs/02・04・06 改訂と docs/08（API一覧）新設（v3.78.2、2026-04-30）
  - docs/02: 全URL遷移・8タブ詳細・申込/キャンセルフロー
  - docs/04: クラス値修正・順位計算・申込/ペア大会/認証/操作ログ17アクション
  - docs/06: 全8環境変数・Google OAuth・Gmail SMTP・デプロイチェックリスト
  - docs/08: 約50エンドポイントを認証要件付きでカテゴリ別列挙
- ✅ docs/01 と docs/03 の全面改訂（v3.78.1、2026-04-30）
  - docs/01: 全URL（認証・申込・マスター系）、8タブ構成、環境変数、役者と権限を最新化
  - docs/03: 全17テーブル + v_results を migration 023 適用後の状態で網羅
  - コード変更なし、仕様理解の正確性向上が目的
- ✅ トップページ左下の隠し管理者ボタンを削除（v3.78、2026-04-23）
  - 旧仕様: 左下 40×40px の透明ボタン → prompt で `repros` 入力 → `/admin` 遷移
  - 今後の管理者アクセスは別途用意するログイン機構経由で行う
- ✅ トップページ大会カードのスマホ最適化（v3.77、2026-04-23）
  - タイトル1行、バッジ+ボタン1行（左右）、会場・日付の縦積みの3ブロック構成
  - `whiteSpace: nowrap` を要所に置き、日本語の途中改行・バッジ割れを防止
  - カード全体が1つの `<Link>`（タッチ領域最大化）
- ✅ 大会記録審査表Excel の改善（v3.76、2026-04-19）
  - 選手なし行は全セル空欄化（連番や0を残さない）
  - 氏名列は本名のみ、摘要列(P)に `賞典外` `失格` `棄権` `CBn` `FRn` をスペース区切りで併記
- ✅ 大会報告書Excel の ④⑤本部納入金出力（v3.76、2026-04-19）
  - O31/Q31 (④ 奨励金納付金), O32/Q32 (⑤ 参加料納付金) に画面計算値を直接書き込み
  - テンプレートの数式(`=O18`, `=1000*O31` 等)を値で上書き
- ✅ 大会詳細→操作ログへのショートカット（v3.75、2026-04-19）
  - AdminDetail ヘッダーに「📋 操作ログ」ボタン（PC=タブ行右端、モバイル=ドロワー内）
  - `/admin/logs?tournament_id={id}` で当該大会に絞り込み済み
  - /admin/logs は URL クエリ `tournament_id` を初期フィルタに反映
- ✅ 大会報告の参加人数は申込管理から集計（v3.74、2026-04-19）
  - `registrations` テーブルから `status = 'active'` のみを class 別に COUNT
  - 1選手=1行（両日参加者も1名としてカウント）
  - AAA クラスも集計対象
- ✅ 閲覧履歴機能を削除（v3.74、2026-04-19）
  - 管理画面「閲覧履歴」タブ・`viewer_logs` テーブル・関連API削除
  - 閲覧者ログイン（行ハイライト用）は残る（ログ記録のみ停止）
  - ⚠️ **migration 023 はNeonで実行済み（viewer_logs DROP）**
- ✅ 全項目の編集は申込管理タブに集約（v3.73、2026-04-19）
  - 選手管理タブ（MembersTab）で編集可能なのは **所属** と **賞典外** のみ
  - 会員番号・クラス・審判は **表示のみ**（input/select/checkbox すべて廃止、誤解防止）
  - 申込管理タブ（RegistrationsTab）で会員番号を編集すると:
    - `registrations` / `members`（両日分、移行済みの場合） / `scores` に自動同期
    - `player_master` は主キーなので触らない
  - 選手マスター未登録の会員番号を入力した場合は確認モーダル表示（v3.69から移植）
  - 申込管理でクラス・審判を変更すると（v3.72既存）:
    - `registrations` / `members`（両日分） / `player_master`（trap_class/skeet_class分岐）に同期
- ✅ クラス・審判の編集は申込管理タブに一本化（v3.72、2026-04-19）
  - player_master は `event_type` に応じて `trap_class` / `skeet_class` を振り分け更新
  - 選手管理API の PATCH は body の `class` / `is_judge` / `member_code` を無視
- ✅ 賞典外を成績一覧に統合（v3.71、2026-04-19）
  - `v_results` を FULL OUTER JOIN 化 → 2日目のみ選手もビューに含まれるようになった（migration 022）
  - 成績確認・閲覧者画面・大会記録審査表Excelで共通化：
    - 通常選手は合計DESC順で rank 連番
    - 賞典外選手は **rank = NULL** で同じ一覧に混在（total DESC で並ぶ）
    - 氏名横に「（賞典外）」を小さく表示
  - 失格・棄権は末尾固定
  - 手動順位設定モーダルは賞典外を除外（対象外）
  - 「組」列は 1日目のみ `N組` / 両日 `N/M組` / 2日目のみ `M組`
  - ⚠️ **migration 022 はNeonで実行済み**
- ✅ 申込管理リストの一括編集モード（v3.70）
  - 「編集」ボタンで全行一斉に編集可能、「保存」で差分PATCH / 「キャンセル」でスナップショット復元
  - 「参加キャンセル」「削除」「申込中に戻す」は編集モード時のみ活性化（誤操作防止）
- ✅ 賞典外フラグは選手管理タブで編集（v3.70、点数登録タブからは削除）
  - 表示モード: `✓` または空白、編集モード時のみチェックボックス活性化
  - 両日参加者は自動同期
- ✅ 赤色テーマ調整（v3.70）: `C.red` を `#ff4d4d`（ハッキリした赤）に変更
- ✅ 会員番号編集機能（v3.69）
  - 選手管理タブの「編集」モードで会員番号列もインライン編集可能
  - 半角数字バリデーション（全角→半角自動変換）
  - 選手マスター未登録コードは警告モーダル表示 → 強制保存可
  - 変更時は `members`（両日分）/ `scores` / `registrations` の `member_code` を一括同期更新
  - API: `PATCH /api/tournaments/[id]/members/[memberId]` が `member_code` パラメータを受け付ける
- ✅ 大会ID表示（v3.69）: 大会設定タブの大会情報セクション上部に「大会ID: X（編集不可）」を表示
- ✅ 所属協会名の短縮統一（v3.68）
  - `player_master.affiliation` / `members.belong` / `registrations.belong` は短縮名で保存（例: `大阪`）
  - 入力時は `toShortName()`（`lib/affiliation.ts`）を通して自動短縮（`大阪府クレー射撃協会` → `大阪`）
  - 正式名称は `associations.formal_name` に保存、または `toFormalName()` で展開
  - 大会申込画面の会場上に主催協会の**正式名称**を表示
  - 記録審査報告書・大会報告書の「主催協会」セルは正式名称を出力
  - 特殊: `北海道`（道残す）/ `東京` / `芸文`（芸能文化人ガンクラブ）
  - マップにない値（例: `学連`）はフォールバックでそのまま保存
  - ⚠️ **migration 021 はNeonで実行済み**

- ✅ 大会管理（作成/編集/削除）
- ✅ 選手管理（インライン編集・DnD組替・ランダム組替・削除・空きスロット表示）
  - ランダム組替: 主催協会の審判を各組の前半・後半に優先配置。不足時は参加人数の多い他協会から審判補充。両日参加者を前方の組に優先配置
  - **前日の成績順組替（v3.63）**: 2日目専用。1日目の順位順に1組から配置。失格・棄権は点数で序列。2日目のみの選手は末尾に申込順で配置
- ✅ 点数入力（テンキーポップアップ、縦移動）
  - **v3.67: 点数保存時に操作ログ記録**（action=`score_save`、detail=`1組R1：氏名1 23、氏名2 17、...`）
  - ステータス変更も記録（`1組ステータス：氏名 失格`）
  - 「この組の点数を保存」ボタン経由はラウンド不明のため未記録
- ✅ CB入力（1〜6専用テンキー）・FR入力（1〜99テンキー）
- ✅ 成績ステータス（有効/失格/棄権）ドロップダウン
- ✅ 賞典外機能（v3.67）
  - `members.is_non_prize` フラグ、点数登録画面にチェックボックス
  - 両方参加者は両日分を同期更新
  - 成績確認・閲覧者画面では 通常選手 → 「── 賞典外 ──」区切り → 賞典外選手 の順
  - `v_results` ビューで賞典外と通常を別パーティションでRANK()
  - ⚠️ **migration 020 はNeonで実行済み**（`db/migrations/020_add_non_prize.sql`）
- ✅ 成績確認（順位・合計・平均・クラス別・CB/FR列条件表示）
- ✅ 失格/棄権：赤文字表示・最下位（rankNULL）・順位なし
- ✅ 順位計算：合計DESC → カウントバック（R8→R1 DESC） → CB ASC → FR DESC（v3.43）
- ✅ 手動順位設定（DnD並び替えモーダル、DB保存、自動計算に戻す）
- ✅ QRコード自動生成（URLコピー機能付き）
- ✅ 閲覧者モード（ログイン不要で成績閲覧可能。ログインは自分の行ハイライト用オプション）
  - **大会一覧ボタン動的変更（v3.64）**: 「概要」→「射順」→「成績」の3段階ボタン（score_count, squad_published_atで判定）
  - 表示順: 2ブロック構成（点数登録済み→合計降順、未登録→組・番号順）（v3.27）
  - フィルター・集計はモーダルポップアップ表示（v3.41）— ヘッダーの「フィルター」ボタンから開閉
  - ヘッダーにボタン行：閲覧QRコード・フィルター・↺ 更新（v3.41）
  - レイアウトは`height:100vh; flex column`構成、スクロールコンテキストは1箇所のみ（v3.41.2）
- ✅ 2日目対応（1日目コピー機能）
- ✅ react-datepicker カレンダー入力
- ✅ ダークテーマ（全画面統一）
- ✅ 選手マスター管理（/admin/players）— 1,370名登録済み（日クレ名簿一括登録）
  - 初回ロード時に全件取得しない（運営管理者は所属で自動フィルター、システム管理者は手動検索）
  - **トラップクラス（trap_class）・スキートクラス（skeet_class）を別々に管理（v3.55）**
  - change_history: 管理者編集時・申込時のクラス・所属変更履歴をテキストで自動記録（v3.55）
- ✅ 選手検索の旧字体→新字体変換（v3.10）— 瀨→瀬、髙→高等の異体字でも検索ヒット
- ✅ 所属フィールド ドロップダウン（協会マスターAPIから取得）
- ✅ インライン編集時にplayer_masterのクラス・審判フラグを自動更新（updated_at記録。v3.55からtrap_class/skeet_classで競技別管理）
- ✅ 申込管理タブ統合（v3.6）+ UI改善（v3.8）+ 編集機能拡張（v3.66）
  - 手動追加エリア: BulkRegisterTab同等のUI（検索・設定→保存、source='manual'でregistrations登録）
  - 各行に「参加」列追加（1日目/2日目/両方を個別選択）、日目切替ボタン廃止
  - 一覧: インライン編集（氏名・所属・クラス・審判・**参加日(v3.66)**）、登録元バッジ（Web/手動）
  - **v3.66: 登録済の申込も編集可能に**（キャンセル済のみ編集不可）
    - 氏名・所属・クラス・審判を編集すると `members` テーブル（両日分）と `player_master`（クラス/審判）も同期更新
    - 参加日変更ルール: `両方↔片日` のみ許可（1日目↔2日目 直接変更は禁止）
    - `両方→片日` の際、削除日に点数があれば拒否
    - `片日→両方` の際、追加日の空きスロットへ自動割当
    - 操作ログ: `registration_edit` アクション追加
  - 申込状況表示: 募集人数・申込中・残り/オーバーを日程別に表示（オーバー時赤字）
  - Web申込者: キャンセルのみ / 手動登録者: 削除（完全削除）可能
  - **申込リスト↔選手管理の双方向連動（v3.52）**:
    - 申込リストでキャンセル → 移行済み（transferred_at IS NOT NULL）の場合、participation_dayに応じてmembersから自動削除。アラート表示
    - 選手管理で削除 → 同member_codeのactive申込を自動キャンセル。成功メッセージに通知
  - 移行ボタン: 未移行N名→選手管理に移行（transferred_at記録、membersに直接INSERT）
  - 移行時オーバーチェック: クライアント・サーバー両方でバリデーション
- ✅ 大会設定/申込設定タブ分離（v3.9）
  - SettingsTab: 大会情報 + QRコード + 危険ゾーン
  - ApplySettingsTab: 申込設定（募集人数・日時・スケジュール・中止お知らせ・注意書き）
  - 各保存ボタン横に「最終保存: {名前} {日時}」表示、リセット実行者も記録・表示
  - 時間フィールドはHH:MM形式で保存（秒なし）
  - 申込完了メールに【中止のお知らせ方法】セクション追加
  - ⚠️ **migration 012 はNeonで実行が必要**（`db/migrations/012_saved_by_tracking.sql`）
- ✅ 管理画面UI改善（v3.11〜v3.12）
  - MembersTab・RegistrationsTab に `maxWidth: 1400, margin: '0 auto'` 追加（横幅統一）
  - スマホ用ハンバーガーメニュー（768px未満）: AdminDetailにisMobile検出 + ドロワー実装
  - ドロワー下部に「Q&A ↗」「お問合せ」「大会一覧」追加（v3.12）
  - 「← 大会一覧」→「← 戻る」(router.back() + /adminフォールバック)（v3.12）
  - 1日開催時はScoresTabの「1日目/2日目」切替タブを非表示（ScoresTabにtournament prop追加）
  - 成績確認・閲覧者画面のクラスフィルター動的化（参加者のクラスのみ表示）（v3.12）
  - 所属フィルターをselectドロップダウンに変更・アルファベット順ソート（v3.12）
- ✅ 選手一括登録タブ（BulkRegisterTab.tsx）は残存するがMembersTabからのリンクは削除済み
- ✅ MembersTab: テーブル形式のインライン編集UI（所属select・クラスselect・審判checkbox → 即時PATCH保存）
- ✅ MembersTab列順: 射順|会員番号|氏名|所属|クラス|審判|操作
- ✅ 共通プログレスインジケータ（LoadingOverlay、画面中央オーバーレイ）
- ✅ 閲覧履歴管理（viewer_logsテーブル、管理者画面「閲覧履歴」タブ）
  - 集計：ユニークユーザー数・総ログイン回数
  - 所属フィルタ・30件ページング
  - ⚠️ **viewer_logsテーブルはNeonで手動作成が必要**（`db/migrations/003_create_viewer_logs.sql`）
- ✅ 管理者認証システム（NextAuth.js v4）
  - システム管理者：Google OAuth（`SYSTEM_ADMIN_EMAILS` 環境変数でメール許可制御）
  - 運営管理者：会員番号＋パスワード（bcrypt rounds=12）
  - `/admin/*` はミドルウェアで保護（未ログイン→`/admin/login` リダイレクト）
  - `tournament_admins` テーブル（`db/migrations/004_admin_auth.sql` 実行済み）
  - 運営管理者マスター画面（`/admin/admins`）— 作成・編集・削除
  - 運営管理者は自所属（ORGANIZERS名一致）の大会のみ表示
  - 大会設定タブ（SettingsTab）に「運営管理者 招待QRコード」セクション追加（v3.28）
  - ⚠️ **Vercel環境変数の設定が必要**（下記「Vercel環境変数」参照）
  - ※ UI表示は「運営管理者」に統一済み（v3.28）。コード内部の変数名・テーブル名（`tournament_admins`等）は従来通り
- ✅ 協会マスター（associations）・射撃場マスター（shooting_ranges）テーブル追加（v3.4）
  - 協会マスター管理画面（/admin/associations）: 射撃場チェックボックス・中止連絡方法・注意書き
  - 射撃場マスター管理画面（/admin/shooting-ranges）: 都道府県グループ表示・追加・削除
  - 所属ドロップダウンはassociation APIから取得（MembersTab・BulkRegisterTab・PlayersPage・申込フォーム）
  - 新規大会作成・SettingsTabの「主催」はassociation APIから取得、「射撃場名」はセレクトボックスに変更
  - ⚠️ **migration 009 はNeonで手動実行が必要**（`db/migrations/009_associations_shooting_ranges.sql`）
- ✅ 大会申込機能（v3.0）
  - **申込フロー（v3.55改修）：会員番号＋メール入力→6桁コード発行（registration_tokens.member_code保存）→Step2自動補完→フォーム送信→完了メール**
  - 会員番号からplayer_masterを検索して氏名・所属・クラス（種目に応じてtrap_class/skeet_class）を自動補完
  - 申込時、player_masterのクラス・所属と差分があればchange_historyに追記
  - キャンセルフロー：メール入力→ワンタイムURL（1時間）→確認→ログ記録
  - 参加日程：1日目/2日目/両方（2日制大会のみ）
  - 残数チェック：1日目・2日目独立カウント（both は両方に消費）
  - 申込管理タブ（RegistrationsTab）：申込一覧・管理者キャンセル・一括登録移行
  - 申込用QRコード（SettingsTabに追加）
  - 大会削除ボタン（SettingsTab 危険ゾーン、CASCADE DELETE）
  - viewer大会一覧に申込ボタン追加（申込期間中のみ表示）
  - ⚠️ **migration 008 はNeonで手動実行が必要**（`db/migrations/008_tournament_registration.sql`）
  - ⚠️ **migration 011 はNeonで実行が必要**（`db/migrations/011_registration_source_transferred.sql`）
  - ⚠️ **Vercel環境変数** `EMAIL_USER`・`EMAIL_PASS` の設定が必要（Gmail SMTP）
- ✅ 記録審査タブ（v3.31）
  - AdminDetail の「成績確認」と「閲覧履歴」の間に配置
  - ルール選択・環境情報・審査担当者・射場設定の4セクション
  - tournamentsテーブルに10カラム追加（migration 015）
  - `components/admin/InspectionTab.tsx`
  - 大会記録審査表Excel出力（v3.32→v3.35全面書き換え）: テンプレート`templates/inspection-report-template.xlsx`使用、ロゴ保持
  - API: `GET /api/tournaments/[id]/inspection-report?date=&classes=`
  - v3.35: **JSZip+XML直接操作方式**（ExcelJS廃止）。printerSettings・pageSetup・数式を完全保持
  - テンプレートは72行（2ページ）対応。行の動的追加不要。改ページはRow 45にrowBreaks設定済み
  - ⚠️ ExcelJSはpackage.jsonに残存（他で使用していないため将来的に削除可能）
- ✅ 大会報告タブ（v3.47）
  - AdminDetail の「記録審査」と「閲覧履歴」の間に配置
  - トラップ＋スキートのペア大会を自動検出（day1_date + organizer_cd）
  - 報告書は1つをペア大会で共有（どちらの大会からも参照・編集可能）
  - 参加人数: クラス別×種目別の自動集計（DISTINCT member_code）
  - 奨励金獲得者: スコアから50/75/100ストレートを自動検出＋手動補正
  - 本部納入金: 自動計算（公認料・広告代・奨励金納付・参加料納付・差引・合計）
  - 会長名: 協会マスター（associations.president_name）と双方向同期
  - Excel出力: `templates/report-template.xlsx` テンプレート使用、JSZip+XML方式
  - Excel出力時: 数式キャッシュクリア(`clearFormulaCache`)必須（テンプレートのサンプルデータ残存防止）
  - 未保存状態でのExcel出力はブロック（アラート表示）
  - `components/admin/ReportTab.tsx`
  - API: `GET/PUT /api/tournaments/[id]/report`, `GET /api/tournaments/[id]/report-excel`
  - DB: `tournament_reports`, `report_incentives` テーブル（migration 017）
  - ⚠️ **migration 017 はNeonで実行済み**
- ✅ ログイン・操作ログ（v3.19→v3.39拡張）
  - `operation_logs` テーブル（FK制約なし＝大会削除後もログ残る）
  - 全16操作を記録：大会CRUD・リセット・申込設定・手動追加・移行・キャンセル・削除・復元・選手削除・ログイン・記録審査保存・記録審査表ダウンロード・大会報告保存・大会報告書ダウンロード
  - ログイン記録：Google OAuth / パスワード認証の両方をoperation_logsに記録
  - ログ閲覧画面（`/admin/logs`）：システム管理者＋運営管理者（自所属のみ）
  - 所属協会フィルター（システム管理者用ドロップダウン）
  - `lib/operation-log.ts` の `writeOperationLog()` で統一的にログ記録
