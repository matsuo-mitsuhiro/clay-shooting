# 開発履歴・変更ログ

## バージョン履歴

---

### v3.47 — 大会報告タブ追加（2026-04-08）
1. 管理画面に「大会報告」タブを新規追加（記録審査と閲覧履歴の間）
2. トラップ＋スキートのペア大会を自動検出（day1_date + organizer_cd で判定）
3. 報告書は1つをペア大会で共有（どちらからも参照・編集可能）
4. 参加人数をクラス別×種目別で自動集計（会員番号ベースのユニークカウント）
5. 奨励金獲得者をスコアデータから自動検出（50/75/100ストレート）＋手動追加・修正可能
6. 本部納入金の自動計算（公認料・広告代・奨励金納付・参加料納付・差引・合計）
7. 会長名は協会マスターと双方向同期（入力→協会マスター上書き保存）
8. 報告日デフォルト＝当日、年度は4月始まりで自動計算
9. Excel出力機能（大会報告書テンプレートベース、JSZip+XML方式）
10. DB: associations.president_name追加、tournament_reports/report_incentivesテーブル新規作成
11. 操作ログ: report_save / report_download アクション追加

---

### v3.42.4 — フィルター変更後に順位1が隠れる問題修正（2026-04-08）
1. classFilter・belongFilter変更時にスクロール位置を先頭(scrollTop=0)にリセット
2. フィルター適用後の表示データ変更でスクロール位置が残る問題を解消

---

### v3.42.3 — ハイライト行スクロールで順位1が隠れる問題修正（2026-04-08）
1. scrollIntoViewを画面外にある場合のみ発動するよう条件追加
2. theadの高さ(40px)を考慮し、行がコンテナ上端+40px以上にある場合のみスクロール
3. スクロール先をblock:'nearest'からblock:'center'に変更（画面中央に配置）

---

### v3.46 — ルール選択肢に「ISSF（本部同等）」を追加（2026-04-08）
1. ルール選択肢に「ISSF（本部同等）」を追加（3つ目の選択肢）

---

### v3.45 — ルール設定の選択肢を変更（2026-04-08）
1. ルール選択肢を「ISSF（地方公式版）」「ビギナー・マスター」の2つに変更
2. 「ISSF（全日本版）」「ATA」「JCSAルール」を削除

---

### v3.44 — 「本大会閲覧用確認」リンクを廃止（2026-04-08）
1. 管理画面ヘッダーの「本大会閲覧用確認 ↗」ボタンを削除（モバイル版・PC版の両方）

---

### v3.43 — カウントバック順位計算の実装（2026-04-08）
1. v_resultsビューの順位計算にカウントバック（最終ラウンドからの逆順比較）を追加
2. ORDER BY: 合計DESC → R8→R7→R6→R5→R4→R3→R2→R1 DESC → CB ASC → FR DESC
3. 1日制（R5〜R8がNULL）でも正常動作（COALESCE(rN, 0)で統一）
4. トラップ・スキート共通ルール
5. migration 016実行済み

---

### v3.42.2 — モーダルは閉じるボタンのみで閉じる方式に変更（2026-04-08）
1. フィルター・QRコードモーダルの背景タップによる閉じ動作を廃止
2. 「閉じる」ボタンのみでモーダルを閉じる方式に統一
3. ゴーストスクロール（背景タップ時に順位1行目が隠れる問題）を根本解消
4. スクロール位置保存・復元ロジックも不要となり削除

---

### v3.42 — 全画面モーダル表示中の背景操作ブロック（2026-04-08）
1. モーダル表示中はheader・mainに`pointerEvents:'none'`を設定し背景操作を遮断
2. 対象: ViewerPage（フィルター・QR）、ScoresTab（テンキー・保存確認）、ResultsTab（手動順位）、AdminDetail（メニュー・問合せ）、InspectionTab（Excelダイアログ）、SupportPage（3モーダル）、AdminsPage（編集）、PlayersPage（編集・削除確認）
3. LoadingOverlayは全画面position:fixedで既にブロック済みのためスキップ

---

### v3.41.4 — モーダル後のヘッダーずれ修正（2026-04-08）
1. ルートを`height:100vh`から`position:fixed; inset:0`に変更
2. iOS Safariのビジュアルビューポートずれを防止（fixedモーダル表示後の問題）

---

### v3.41.3 — スクロール引っかかり修正（2026-04-07）
1. `-webkit-overflow-scrolling: touch` を削除（iOS13+では不要、sticky競合の原因）
2. `<tr>` の `transition: background` を削除（スクロール中のレイアウト計算負荷軽減）

---

### v3.41.2 — 二重スクロール問題・sticky thead消失を根本修正（2026-04-07）
1. ルートを`height:100vh; overflow:hidden; flex column`に変更しページレベルスクロールを排除
2. スクロールコンテキストをテーブルコンテナ1箇所に統一（二重スクロールの引っかかり解消）
3. headerを`position:sticky`から通常配置に変更（flex shrink:0で上部固定）
4. `headerH`動的計算・`maxHeight: calc()`を廃止し`flex:1`で残高さを自動取得
5. モーダル後のsticky再描画ハックも不要になり削除

### v3.41.1 — モーダル後のsticky thead消失バグ修正（2026-04-07）
1. QRコード・フィルターモーダル表示中に`body.overflow=hidden`で背面スクロール防止
2. モーダルを閉じた後にテーブルコンテナを強制再描画（`display:none→復帰`）でsticky再計算を誘発
3. iOS Safariの`position:fixed`+`position:sticky`競合バグに対応

---

### v3.41 — 閲覧者ページ：フィルターをポップアップモーダル化（2026-04-07）
1. フィルター・集計をインライン表示からモーダルポップアップに変更
2. ヘッダーにボタン行追加：「閲覧QRコード」「フィルター」「↺ 更新」
3. フィルターボタンはフィルター適用中にゴールドでハイライト表示
4. テーブルのmaxHeightをヘッダー高さのみで固定計算（動的再計算不要に）
5. テーブル表示位置がフィルター開閉で変動しなくなり、sticky thead問題を根本解消
6. フィルターモーダル内にリセットボタン追加

---

### v3.40 — 閲覧者ページのテーブル表示修正（2026-04-07）
1. テーブルコンテナの`maxHeight`/`overflow:auto`を廃止（二重スクロール問題解消）
2. ページ全体スクロール＋`position: sticky` theadに変更（参照システムのCSS設計を参考）
3. theadの`top`を`headerH + toggleH`に設定し、ビューポート基準で固定
4. フィルター展開/閉じに関係なく全行が表示されるように

---

### v3.39 — ログイン・操作ログ機能改修（2026-04-07）
1. **OperationAction に3種追加**: `login`, `inspection_save`, `inspection_download`
2. **ログインイベントをoperation_logsに記録**: Google OAuth / パスワード認証の両方
3. **記録審査の保存操作をログ記録**: `_save_type=inspection` 時に `inspection_save` アクションで記録
4. **Excelダウンロード操作をログ記録**: `inspection_download` アクションで記録（クラス情報付き）
5. **メニュー名称変更**: 「操作ログ」→「ログイン・操作ログ」（ダッシュボード + ページタイトル）
6. **テーブル列名変更**: 「所属団体」→「所属協会」
7. **所属協会フィルター追加**: システム管理者用ドロップダウン（associations APIから取得）
8. **運営管理者のアクセス許可**: 自所属のログのみ閲覧可能（APIで強制フィルター）
9. **フォントサイズ拡大**: h1=20px, th/td=15px, バッジ=14px, フィルター=15px, ページング=15px
10. **ログインバッジ色分け**: ログイン操作は青色で表示

---

### v3.38 — Excel共有数式の孤児参照エラー修正（2026-04-07）
1. I列・O列の全72行を値で上書きし、共有数式（shared formula）の孤児参照を防止
2. Excelの「一部の内容に問題が見つかりました」警告を解消

---

### v3.37 — Excelダイアログのフォントサイズ拡大・ボタン名変更（2026-04-07）
1. ダイアログ内の全要素を2px拡大（タイトル19px, ラベル15px, 入力16px, 注意書き14px, ボタン17px）
2. 「作　成」→「ダウンロード」に変更

---

### v3.36 — Excel記録審査表の集計・ラベル・注意書き改善（2026-04-07）
1. **I列・N列・O列に集計値を直接書き込み**（数式ではなくDB計算済みの値を使用）
2. **ラベル変更**: 「ﾄﾗｯﾌﾟセットNo.」→「セットNo.」（L8セル）
3. **セット表記変更**: `1日目:2` → `2`、`1日目:2 / 2日目:8` → `2/8`
4. **ダイアログに注意書き追加**: 点数修正はExcel側で行わず再ダウンロードする旨の警告

---

### v3.35 — Excel出力をZIP/XML直接操作方式に全面書き換え（2026-04-07）
1. **ExcelJS → JSZip + XML直接操作に変更**
   - ExcelJSがprinterSettings・pageSetup等を破壊する問題を根本解決
   - テンプレートのXML構造・プリンタ設定・改ページを完全保持
   - インライン文字列（`t="inlineStr"`）で文字列セルを書き込み
   - I列（SUM数式）・O列（IF数式）を保持し、calcChain.xml削除で再計算強制
2. **新テンプレート（72行対応）に差し替え**
   - テンプレートに72行（2ページ分）のデータ行を事前定義
   - Row 45に改ページ（rowBreaks）設定済み
   - 行の動的追加が不要に
3. **依存パッケージ**: `jszip` 追加（ExcelJSも残存、将来的に削除可能）

---

### v3.34 — 大会記録審査表Excel印刷プレビュー修正（2026-04-07）
1. **新テンプレート対応**: ユーザー提供の更新テンプレートに差し替え
   - S列を表示化（非表示解除）、不要な文字削除
   - 一般社団法人 日本クレー射撃協会の文字をページヘッダー/フッターに移動（Row 46削除）
2. **API修正** (`inspection-report/route.ts`):
   - S列への書き込みを削除（クラスデータ不要）
   - pageSetup更新: scale 97→100, fitToPage true, fitToWidth 1, fitToHeight 0
   - printArea: A1:R46 → A1:R45（フッター行削除に対応）
   - 未使用行クリアからS列除外

---

### v3.33 — 全画面フッターにバージョン表記追加（2026-04-07）
1. **全画面共通フッター**: `© 2026–present Mitsuhiro Matsuo. All rights reserved.　Ver.X.XX`
2. バージョン番号を `lib/version.ts` で一元管理（`APP_VERSION`）
3. Footer未使用だった12画面にFooterコンポーネントを追加
   - トップ・ログイン・登録・パスワードリセット・FAQ・サポート・申込・キャンセル・管理者詳細

---

### v3.32 — 大会記録審査表Excel出力機能（2026-04-07）
1. **記録審査タブに「大会記録審査表 Excel」ボタン追加**
   - ダイアログで作成日（カレンダー選択）とクラス選択（分けありの場合のみ）
   - Excelテンプレート（TRAPAB）にデータを埋め込みダウンロード
   - ロゴ画像・書式・数式を保持
   - 36名超は行を自動追加
   - 摘要欄にCB/FR値を記載（CB１FR50形式）
   - 種目: T　トラップ / S　スキート
   - クラス: 選択クラスの結合表記（AA/A等）
2. **テンプレートファイル**: `templates/inspection-report-template.xlsx`
3. **API**: `GET /api/tournaments/[id]/inspection-report?date=&classes=`

---

### v3.31 — 記録審査タブ追加＋選手マスター改善＋日クレ名簿一括登録（2026-04-07）
1. **記録審査タブ新設**（AdminDetailの「成績確認」と「閲覧履歴」の間に配置）
   - ルール選択（ISSF地方公式版/全日本版/ATA/JCSAルール）
   - 環境情報（天候・気温・風速）
   - 審査担当者（審査委員長・大会運営責任者・記録責任者・セット確認者）
   - 射場設定（使用クレー名・トラップセットNo.表示・クラス分け選択）
   - DB: tournamentsテーブルに10カラム追加（migration 015実行済み）
2. **選手マスター画面改善**
   - 初回ロード時に全件取得しない（検索ボタンで取得に変更）
   - 運営管理者: ログインユーザーの所属で自動フィルター＋自動検索
   - システム管理者: フィルターなし（手動検索）
   - フィルター条件なしのAPI呼び出しは空配列を返す
3. **日クレ名簿一括登録**: 1,370名のデータをplayer_masterにupsert（名前のみ上書き）
4. **prefectures.ts**: 学連（cd: 98）を追加

---

### v3.30 — 閲覧者画面ヘッダー・フィルター・テーブルヘッダーを上部固定（2026-04-07）
1. **閲覧者画面**: 3層sticky構造に変更
   - ①ヘッダー（大会名等）: `position: sticky; top: 0`
   - ②フィルタートグルボタン: `position: sticky; top: ヘッダー高さ`（B案: 展開中身はスクロール）
   - ③テーブルヘッダー: `position: sticky; top: ヘッダー+トグル高さ`
2. ヘッダー・トグルの高さをrefで動的測定（レスポンシブ対応）
3. mainのoverflow:auto / maxHeight削除（ページ全体スクロールに変更）
4. ハイライト行スクロールを`block:'center'`→`block:'nearest'`に変更

---

### v3.29 — 閲覧者画面フィルター・集計の折りたたみ（2026-04-06）
1. **閲覧者画面**: フィルター（クラス・所属協会）と集計（平均・件数）エリアを折りたたみ式に変更
   - スマホ（768px未満）: 初期状態は閉じた状態
   - PC（768px以上）: 初期状態は開いた状態
   - 「▼ フィルター・集計」トグルボタンで開閉
   - 閉じた状態でもフィルター適用中はバッジ表示

---

### v3.28 — 文言統一「運営管理者」＋大会設定タブに招待QRコード追加（2026-04-06）
1. **文言統一**: UI表示の「大会管理者」→「運営管理者」に全画面統一
   - 対象: ログイン画面・ヘッダーバッジ・管理者マスター画面・登録フォーム・メール・FAQ・マニュアル
   - 「システム管理者」は変更なし
   - コード内部（変数名・テーブル名・ロール値）は変更なし
2. **大会設定タブ（SettingsTab）に「運営管理者 招待QRコード」セクション追加**
   - 「📱 招待QRコードを発行」ボタンでトークン発行→QRコード表示
   - URL表示・コピー機能付き
   - 注意書き: 使用期限24時間・1名限定・他府県所属の説明

---

### v3.27 — 閲覧者画面の表示順を2ブロック構成に変更（2026-04-05）
1. **閲覧者画面**: 表示順を「点数登録済み（上部）→ 未登録（下部）」の2ブロック構成に変更
   - 点数登録済み選手 → 合計点降順（順位表示）
   - 点数未登録選手 → 組・番号順（1組1番→1組2番→…）
   - 変更前（v3.25）: 組単位ソート（組ごとにまとめて点数あり→降順・なし→番号順）

---

### v3.26 — 選手マスターの会員番号編集を可能に（2026-04-05）
1. 編集モーダルで会員番号フィールドを編集可能に変更（従来は読み取り専用）
2. PUT API に `new_member_code` パラメータ追加し、`player_master` の主キーを更新可能に

---

### v3.25 — 閲覧者画面の表示順改善（組単位ソート）（2026-04-05）
1. **閲覧者画面**: 表示順を組単位でソートするよう変更
   - 点数未入力の組 → 組内番号（position）昇順で表示
   - 点数入力済みの組 → 合計点降順で表示
   - 組全体は常に組番号昇順（1組→2組→…）
2. **v_results ビューに `position` カラム追加**（migration 014）
3. **Result型に `position: number` 追加**

---

### v3.24 — 操作マニュアル作成（VitePress）（2026-04-04）
1. **VitePressで操作マニュアルサイトを構築**: `/manual/` でアクセス可能
2. **全11ページ作成**: はじめに、成績を見る、大会を作成する、申込を管理する、選手を登録する、成績を入力する、結果を公開する、管理者アカウントを管理する、協会マスターを管理する、射撃場マスターを管理する、Q&Aを管理する
3. **全ボタン名・項目名をソースコードから正確に抽出して記載**

---

### v3.23 — 選手マスター・申込管理のstickyヘッダー＋横スクロール（2026-04-04）
1. **選手マスター**: テーブルヘッダー行をsticky固定（下スクロール時）
2. **申込管理リスト**: テーブルヘッダー行をsticky固定、横スクロール追加、氏名列＋所属協会列をsticky left固定

---

### v3.22 — 閲覧者画面のテーブルヘッダーをsticky固定（2026-04-03）
1. **閲覧者画面**: theadにposition: sticky; top: 0を適用。下スクロール時にヘッダー行が画面上端に固定される

---

### v3.21 — sticky列追加（閲覧者の順位列・選手管理の番号+氏名列）（2026-04-03）
1. **閲覧者画面**: 順位列+氏名列の2列をsticky固定
2. **選手管理**: 番号列+氏名列の2列をsticky固定

---

### v3.20 — UI改善5件（QRタブ化・ログイン画面・番号表示・成績移動・sticky列）（2026-04-03）
1. **QRコード確認をタブ切り替え方式に変更**: 3つ横並び→タブ切替（デフォルト閲覧者用QR）。スマホでのはみ出し・カメラ干渉を解消
2. **ログイン画面のシステム管理者ログインを目立たなくする**: Googleボタンを非表示にし「システム管理者の方はこちら」リンクで展開
3. **点数入力画面の氏名欄に番号表示**: `1. 西奥 直樹` のようにposition番号を氏名の前に表示
4. **成績ステータスを点数入力→選手管理に移動**: 点数入力の「成績」列を削除→代わりに「所属協会」表示。選手管理に「成績」列（有効/失格/棄権）を追加。新規API `/api/tournaments/[id]/scores/status`
5. **氏名列を左固定（sticky）5画面**: 点数入力・成績確認・閲覧者・選手マスター・大会管理者マスター。選手マスター・大会管理者マスターは横スクロールも新規追加

---

### v3.19 — 操作ログ機能追加（2026-04-03）
1. **`operation_logs` テーブル新規作成**（`db/migrations/013_operation_logs.sql`）: FK制約なし＝大会削除後もログ残る
2. **ログ記録ユーティリティ** `lib/operation-log.ts`: `writeOperationLog()` 共通関数
3. **全11操作にログ記録を組み込み**: 大会作成・更新・削除・リセット、申込設定、手動追加・移行・キャンセル・削除・未移行に戻す、選手削除
4. **ログ閲覧API** `/api/operation-logs`: システム管理者のみ。フィルタ・ページング対応
5. **ログ閲覧画面** `/admin/logs`: 操作種別フィルター、50件ページング、削除系は赤字表示
6. **管理画面ダッシュボードに「操作ログ」ボタン追加**（システム管理者のみ）

---

### v3.18 — UI改善4件（文字置換・視認性・削除警告・フィルター）（2026-04-03）
1. **文字置換**: 「所属」→「所属協会」、「1日目日付」→「1日目」、「2日目日付」→「2日目」、「中止お知らせ方法」→「中止のお知らせ方法」をシステム全体で一括置換
2. **申込管理リスト視認性改善**: キャンセル行のopacity 0.5→0.7、移行済み行のopacity 0.6→0.85
3. **手動登録の削除＋警告**: 全ステータスで「削除」ボタンを表示。キャンセル・削除時に選手管理に登録されている場合は「○日目 ○組○番に登録されている選手です」と警告
4. **申込管理リストにフィルター追加**: クラスフィルター（全て/AA/A/B/C）、所属協会フィルター（ドロップダウン）

---

### v3.13 — モバイルUI修正3件（スクロールリセット・協会マスター・閲覧者ログイン保持）（2026-04-03）
1. **`app/admin/page.tsx` — 自動スクロールトップ修正**:
   - `useRef(orgInitRef)` を追加し、`session`+`associations` の useEffect が1回のみ実行されるよう制御
   - 連鎖的 setState による iOS Safari スクロールリセットを防止
   - モバイルヘッダー整理: 640px未満では氏名・役職バッジを非表示、タイトルを短縮
2. **`app/admin/associations/page.tsx` — スマホ用リスト→詳細遷移**:
   - `isMobile`（768px未満）で UI を切り替え
   - スマホ: 協会一覧（タップで詳細へ）→ 詳細フォーム（ヘッダー「← 戻る」で一覧に戻る）
   - PC: 既存の2カラムレイアウトを維持
   - 射撃場チェックボックスをスマホでは全展開（maxHeight制限なし）・タップしやすいサイズに拡大
   - 保存ボタンをスマホでフルwidth表示
3. **`components/viewer/ViewerPage.tsx` — プルリフレッシュでログイン維持**:
   - `sessionStorage` に `viewer_session_{tournamentId}` としてログイン情報を保存
   - ページリロード時（プルリフレッシュ含む）に sessionStorage から復元しログイン画面をスキップ
   - ログイン成功時に `sessionStorage.setItem()` で保存、タブを閉じると自動クリア

---

### v3.12 — フィルター改善・ハンバーガーメニュー拡張・戻るボタン（2026-04-03）
1. **ハンバーガーメニュー拡張** (`AdminDetail.tsx`):
   - ドロワー下部に区切り線＋「Q&A ↗」「お問合せ」「大会一覧」を追加
   - お問合せはインライン ContactModal コンポーネントで実装（送信API: /api/support/request-token）
2. **「戻る」ボタン化** (`AdminDetail.tsx`):
   - 「← 大会一覧」→ 「← 戻る」に変更
   - `window.history.length > 1` なら `router.back()`、ブックマーク直接アクセス時は `/admin` フォールバック
3. **クラスフィルター動的化** (`ResultsTab.tsx`, `ViewerPage.tsx`):
   - 固定 `['all','AA','A','B','C']` → 参加者に存在するクラスのみを動的表示
   - `(['AA','A','B','C']).filter(c => results.some(r => r.class === c))` で生成
4. **所属フィルターをselectに変更** (`ResultsTab.tsx`, `ViewerPage.tsx`):
   - ボタン横並び → `<select>` ドロップダウンに変更（スマホ対応・多所属でも溢れない）
   - 参加者の所属のみ表示（既存の動的生成ロジックは維持）、アルファベット順ソート追加

---

### v3.11 — 管理画面UI改善（横幅統一・ハンバーガーメニュー・1日大会対応）（2026-04-03）
1. **管理画面 max-width 統一**:
   - `MembersTab.tsx`: 外側divに `maxWidth: 1400, margin: '0 auto'` 追加
   - `RegistrationsTab.tsx`: 同上
   - 他タブ（ScoresTab=1060, ResultsTab=1200, SettingsTab=700, ApplySettingsTab=700, ViewerHistoryTab=900）は既存値を維持
2. **スマホ用ハンバーガーメニュー（768px未満）**:
   - `AdminDetail.tsx`: `isMobile` state（window resize リスナー）を追加
   - モバイル時: タブバー非表示→「☰ + 現在タブ名 + 閲覧用ボタン」バーを表示
   - ☰タップで左からドロワー表示（全タブ一覧・現在タブ強調・オーバーレイ背景）
   - PC時: 従来の横並びタブを維持
3. **1日開催時の日目タブ非表示（ScoresTab）**:
   - `ScoresTab.tsx`: Props に `tournament: Tournament | null` 追加
   - `hasTwoDays = !!(tournament?.day2_date)` で判定
   - 1日開催時は「1日目(R1-4)」「2日目(R5-8)」ボタンを非表示にし、組フィルタのみ表示
   - `AdminDetail.tsx`: ScoresTabに `tournament` を渡すよう変更

---

### v3.10 — 選手検索の旧字体→新字体変換対応（2026-04-02）
1. **lib/kanji-normalize.ts 新規作成**: 旧字体→新字体変換テーブル（約130文字）+ `normalizeKanji()` 関数
   - Unicode NFKC正規化（全角英数→半角英数変換含む）
   - 人名頻出の旧字体・異体字を新字体に変換（髙→高、﨑→崎、瀨→瀬、齋→斎 等）
2. **app/api/players/route.ts**: `q_name` 検索で旧字体→新字体変換後の文字列でもOR検索
3. **components/admin/RegistrationsTab.tsx**: `handleManualSearch()` の氏名一致判定に `normalizeKanji` 適用
4. **components/admin/BulkRegisterTab.tsx**: 同上
5. **app/api/tournaments/[id]/viewer-login/route.ts**: 閲覧者ログインの氏名一致判定に `normalizeKanji` 適用

---

### v3.9 — 大会設定/申込設定タブ分離・保存者表示・メール改善（2026-04-03）
1. **タブ分離**: SettingsTabを「大会設定」と「申込設定」に分離
   - `SettingsTab.tsx`: 大会情報（主催・大会名・射撃場・種目・日付・セット番号）+ QRコード + 危険ゾーン
   - `ApplySettingsTab.tsx` 新規作成: 申込設定（募集人数・日時・スケジュール・中止お知らせ・注意書き）
   - `AdminDetail.tsx`: TabTypeに`apply-settings`追加、タブ順を「大会設定/申込設定/申込管理/...」に変更
2. **保存者名・日時の表示**:
   - DB: `tournaments`テーブルに`info_saved_at`, `info_saved_by`, `apply_saved_at`, `apply_saved_by`, `reset_at`, `reset_by`カラム追加（マイグレーション012）
   - API: PUT /api/tournaments/[id]で`_save_type`パラメータにより保存種別判定、セッションからユーザー名取得
   - API: POST /api/tournaments/[id]/resetでreset_at/reset_by記録
   - UI: 各保存ボタン横に「最終保存: {名前} {YYYY/MM/DD HH:mm}」表示、危険ゾーンに「最終リセット」表示
3. **時間フィールドの秒除去**: gate_open_time等の時間フィールドをHH:MM形式で保存（API側でslice(0,5)）
4. **申込完了メールに中止お知らせ方法追加**:
   - `lib/email.ts`: sendApplyConfirmationの引数に`cancellation_notice`追加、【注意事項】の前に【中止のお知らせ方法】セクション表示
   - `app/api/tournaments/[id]/apply/submit/route.ts`: cancellation_noticeをメール送信に渡すよう修正
5. **lib/types.ts**: Tournament型に`info_saved_at`, `info_saved_by`, `apply_saved_at`, `apply_saved_by`, `reset_at`, `reset_by`追加

---

### v3.8 — 申込管理UI改善（2026-04-02）
1. **SettingsTab**: 「参加人数上限」ラベルを「募集人数」に変更
2. **RegistrationsTab 手動追加エリア改修**:
   - 各行に「参加」列（セレクトボックス: 1日目/2日目/両方）を追加
   - ManualRowにparticipation_dayフィールド追加（デフォルト: day1）
   - 「登録先：1日目/2日目」切替ボタンを廃止（各行で個別に参加日程を選択可能に）
   - manualDay stateを削除
3. **申込状況表示の拡張**:
   - 募集人数・申込中・残り（またはオーバー）を日程別に表示
   - 2日開催の場合は1日目・2日目それぞれの状況を表示
   - max_participantsがnullの場合は「制限なし」と表示
   - オーバー時は赤文字で表示
4. **「更新」ボタン改善**: テキストを「最新に更新」に変更、title属性でツールチップ追加
5. **オーバー時の移行ブロック**:
   - クライアント側: 移行ボタンクリック時に各日程の定員オーバーをチェック、オーバー時はエラー表示
   - サーバー側（transfer API）: 同様のバリデーション追加
6. **AdminDetail**: RegistrationsTabにtournament propを追加

---

### v3.7 — 選手登録→選手管理リネーム・MembersTab UIリニューアル（2026-04-02）
1. **名称変更**: 全UI・コメントで「選手登録」→「選手管理」にリネーム
   - AdminDetail.tsx: タブ名「選手管理」
   - RegistrationsTab.tsx: 移行ボタン「選手管理に移行」
   - ScoresTab.tsx: 未登録警告メッセージ
   - transfer/route.ts: コメント
2. **MembersTab.tsx 大幅リニューアル**:
   - 入力フォーム形式（空スロット＋input行）を廃止
   - テーブル形式の選手一覧に変更（組ごとにポジション1-6を表示）
   - 所属・クラス・審判フラグ: インライン編集（select/checkbox → 即時PATCH保存）
   - 「この組を保存」ボタン廃止（個別フィールド即時保存に変更）
   - 組タブに人数表示（例: 「1組（5名）」）
   - 削除ボタン: 確認ダイアログ追加（スコアあり/なし両方）
   - 選手組替（DnD）・1日目コピー・組追加は維持
3. **新規APIエンドポイント**: `PATCH /api/tournaments/[id]/members/[memberId]`
   - belong, class, is_judgeをインライン更新
   - player_masterのclass・is_judgeも同期更新
4. **削除した機能**:
   - 空スロットの入力フォーム（新規選手を直接追加する機能）
   - 会員番号入力→自動補完（lookupPlayer）
   - 「この組を保存」ボタン（一括保存）
   - 「登録済み選手一覧」セクション（メインテーブルに統合）

---

### v3.6 — 申込管理統合・選手一括登録ボタン削除（2026-04-02）
1. **DB**: `registrations`テーブルに`source`（VARCHAR(10), DEFAULT 'web'）・`transferred_at`（TIMESTAMPTZ）カラム追加（マイグレーション011）
2. **lib/types.ts**: `Registration`型に`source: 'web' | 'manual'`・`transferred_at: string | null`追加
3. **RegistrationsTab 大幅改修**:
   - 手動追加エリア新設（日目選択、+追加6行、検索・設定、保存ボタン、BulkRegisterTab同等のUI）
   - 一覧表示にインライン編集機能追加（氏名・所属・クラス・審判フラグをクリック編集）
   - 登録元（Web/手動）バッジ表示
   - Web申込者: キャンセルボタンのみ / 手動登録者: 削除ボタン（完全削除）
   - 移行済み行（transferred_at != NULL）はグレー表示・編集不可
   - 移行ボタン表示: 「未移行 N名 → 選手管理に移行」（N=0なら非活性）
4. **新規API**: `DELETE /api/tournaments/[id]/registrations/[regId]`（source='manual'のみ削除可能）
5. **新規API**: `PATCH /api/tournaments/[id]/registrations/[regId]`（インライン編集用）
6. **既存API修正**: `POST /api/tournaments/[id]/registrations`（source='manual'で手動登録対応）
7. **既存API修正**: `POST /api/tournaments/[id]/registrations/transfer`
   - `transferred_at IS NULL AND status = 'active'`のみ移行対象
   - 移行後に`transferred_at = NOW()`を記録
   - membersテーブルに直接INSERT（空きスロット自動割り当て）
8. **MembersTab**: 「選手一括登録」ボタン削除、showBulk状態削除
9. **AdminDetail**: bulkタブ・BulkRegisterTabインポート削除
10. **MembersTab横幅統一**: maxWidth: 900の制約を削除（他タブと同等の幅に統一）

---

### v3.4 — 協会・射撃場マスター実装（2026-04-02）
1. **DB**: `associations`テーブル追加（48件: 47都道府県+芸文、cancellation_notice・notes）
2. **DB**: `shooting_ranges`テーブル追加（68件の射撃場初期データ）
3. **DB**: `association_shooting_ranges`中間テーブル追加（マイグレーション009）
4. **lib/types.ts**: `Association`型・`ShootingRange`型追加
5. **新規APIルート**
   - `GET /api/associations` — 全協会一覧（cd昇順、99は末尾）
   - `GET /api/associations/[cd]` — 1件取得（shooting_range_ids[]含む）
   - `PUT /api/associations/[cd]` — 更新（cancellation_notice, notes, shooting_range_ids）
   - `GET /api/shooting-ranges` — 全射撃場一覧（id順）
   - `POST /api/shooting-ranges` — 新規作成（system adminのみ）
   - `DELETE /api/shooting-ranges/[id]` — 削除（system adminのみ）
6. **新規管理画面**
   - `app/admin/shooting-ranges/page.tsx` — 射撃場マスター（都道府県グループ表示、system adminのみ追加・削除）
   - `app/admin/associations/page.tsx` — 協会マスター（射撃場チェックボックス・中止連絡・注意書き編集）
7. **app/admin/page.tsx**: ORGANIZERS削除→API取得、射撃場名テキスト→セレクトボックス、管理ボタンに「射撃場マスター」「協会マスター」追加
8. **components/admin/SettingsTab.tsx**: ORGANIZERS削除→API取得、射撃場名テキスト→セレクトボックス、申込設定初期値を協会マスターから取得
9. **所属ドロップダウンをAPI取得に変更**: MembersTab・BulkRegisterTab・app/admin/players/page.tsx・app/tournaments/[id]/apply/[token]/page.tsx

---

### v3.0 — 大会申込機能実装（2026-04-02）
1. **DB**: `tournaments`テーブルに申込設定カラム11個追加（max_participants, apply_start_at, apply_end_at, cancel_end_at, competition_start_time, gate_open_time, reception_start_time, practice_clay_time, cancellation_notice, notes, apply_qr）
2. **DB**: 新テーブル3つ追加（マイグレーション008）
   - `registrations`（申込データ: member_code, name, belong, email, participation_day, status, cancelled_*）
   - `registration_tokens`（ワンタイムURL: purpose='apply'|'cancel', 1時間有効）
   - `registration_logs`（ログ: log_type='duplicate_error'|'cancel_by_user'|'cancel_by_admin'）
3. **lib/types.ts**: `ParticipationDay`型・`Registration`型・`RegistrationToken`型追加、`Tournament`に申込フィールド追加
4. **lib/email.ts**: `sendApplyToken`・`sendApplyConfirmation`・`sendCancelToken`関数追加（nodemailer + Gmail SMTP）
5. **新規APIルート群**
   - `GET /api/tournaments/[id]/apply-info` — 公開・申込数付き大会情報
   - `POST /api/tournaments/[id]/apply/request-token` — 申込トークン生成・メール送信
   - `POST /api/tournaments/[id]/apply/submit` — 申込登録（重複チェック・ログ）
   - `POST /api/tournaments/[id]/cancel/request-token` — キャンセルトークン生成・メール送信
   - `POST /api/tournaments/[id]/cancel/submit` — キャンセル実行・ログ記録
   - `GET /api/tournaments/[id]/registrations` — 管理者: 申込一覧取得
   - `POST /api/tournaments/[id]/registrations/[regId]/cancel` — 管理者キャンセル
   - `POST /api/tournaments/[id]/registrations/transfer` — 一括登録移行（募集終了後のみ）
6. **新規公開ページ群**
   - `app/tournaments/[id]/apply/page.tsx` — 大会案内・メール入力フォーム
   - `app/tournaments/[id]/apply/[token]/page.tsx` — 申込フォーム（参加日程・氏名・所属・クラス入力）
   - `app/tournaments/[id]/cancel/page.tsx` — キャンセル用メール入力
   - `app/tournaments/[id]/cancel/[token]/page.tsx` — キャンセル確認画面
7. **新規コンポーネント**: `RegistrationsTab.tsx`（申込一覧・管理者キャンセル・一括移行ボタン）
8. **AdminDetail.tsx**: 「申込管理」タブ追加
9. **SettingsTab.tsx**: 申込設定フォーム・申込用QR・大会削除ボタン追加
10. **BulkRegisterTab.tsx**: 参加日程列追加・定員チェック・「両方」保存時に2レコード自動作成
11. **viewer大会一覧**: 申込期間中の大会に「申込」ボタン追加
12. **middleware.ts**: `/tournaments/*` を認証不要（public）に設定

---

### v2.9 — 成績順位手動設定・FR優先順位変更（2026-04-02）
1. **DB**: `scores`テーブルに`manual_rank`カラム追加（マイグレーション007）
2. **v_results ビュー更新**: 順位計算を `合計DESC → FR DESC → CB ASC` に変更（FR優先）
3. **v_results ビュー更新**: `manual_rank`が設定済みの場合は自動計算より優先
4. **新規API**: `PUT /api/tournaments/[id]/scores/ranking`（手動順位一括保存）
5. **ResultsTab**: 「成績順位手動設定」ボタン追加、ドラッグ&ドロップモーダル実装
6. **ResultsTab**: 失格・棄権はモーダル最下部固定（ドラッグ不可・順位なし）
7. **ResultsTab**: 手動設定中バッジ表示・「自動計算に戻す」ボタン実装

---

### v2.8 — CB・FR・成績（失格/棄権）機能追加（2026-04-02）
1. **DB**: `scores`テーブルに`cb`（SMALLINT 1〜6）・`fr`（SMALLINT 1〜99）・`status`（valid/disqualified/withdrawn）カラム追加
2. **v_results ビュー更新**: 有効選手のみで順位付与（合計DESC→CB ASC→FR DESC）、失格/棄権は rank=NULL
3. **lib/types.ts**: `Score`・`ScoreInput`・`Result`型にcb/fr/status追加、rankをnullable化
4. **ScoresTab**: CBテンキー（1〜6専用）・FRテンキー（最大99）・成績ドロップダウン（有効/失格/棄権）追加
5. **ScoresTab**: 失格/棄権選手の行を薄赤背景・点数入力ボタン無効化
6. **ResultsTab**: CB/FR列を値がある時のみ条件表示（凡例付き）、失格/棄権は赤文字＋ラベル表示

---

### v2.1 — 管理者認証システム実装（2026-04-01）
1. **NextAuth.js 導入**（`next-auth@4`, `bcryptjs`）
2. **認証方式**
   - システム管理者：Google OAuth（`SYSTEM_ADMIN_EMAILS` 環境変数で許可メール制限）
   - 大会管理者：会員番号＋パスワード（bcrypt rounds=12）
3. **新規テーブル**（`db/migrations/004_admin_auth.sql`）
   - `tournament_admins`（id, member_code, name, email, password_hash, is_active）
   - `admin_logs`（ログイン履歴）
   - `tournaments.is_public` カラム追加
4. **新規ファイル**
   - `lib/auth.ts` — NextAuth設定（Google Provider + Credentials Provider）
   - `middleware.ts` — `/admin/*` ルート保護
   - `app/admin/layout.tsx` — SessionProvider ラッパー
   - `app/admin/login/page.tsx` — ログイン画面（Googleログイン + 会員番号フォーム）
   - `app/api/auth/[...nextauth]/route.ts` — NextAuth APIルート
   - `app/api/admin/admins/route.ts` — 管理者CRUD API
   - `app/admin/admins/page.tsx` — 大会管理者マスター画面
   - `types/next-auth.d.ts` — 型拡張（role, affiliation, member_code）
5. **既存ファイル更新**
   - `app/admin/page.tsx` — ヘッダーにユーザー情報・ログアウトボタン、大会管理者は自所属のみ表示
   - `components/admin/AdminDetail.tsx` — ヘッダーにユーザー情報・ログアウトボタン追加
   - `lib/types.ts` — `TournamentAdmin`型・`Tournament.is_public`追加

---

### v2.0 — 閲覧ログインフォーム改善・大会一覧日付表示改善（2026-04-01）
1. **ViewerLoginForm 改善**
   - 所属を必須項目に変更（*マーク追加）
   - テキスト変更：「以下を入力してログインしてください」→「所属と氏名を登録して成績を確認してください。」
   - 氏名プレースホルダー（例：松尾）削除
   - 氏名説明文（名前の一部でも入力できます…）削除
   - 大会名の下に開催日表示（2日ある場合は「2026年5月1日 / 2026年5月2日」形式）
   - 「所属と氏名を保存する」チェックボックス追加（localStorage保存・次回自動入力）
2. **大会一覧の日付表示改善** — 閲覧者用・管理者用の両方
   - 2日目がある場合は「2026年5月1日 / 2026年5月2日」形式で表示
3. **ログインユーザー自動ハイライト**
   - ログイン後、入力した氏名・所属で成績テーブルの該当行を自動ハイライト（金色）
   - 該当行へ自動スクロール

---

### v1.9 — 複数改修（2026-03-31）
1. **所属デフォルトをブランクに変更** — BulkRegisterTab・MembersTab（1日目・2日目）の3か所
   - 選択肢先頭にブランク（`—`）を追加、デフォルト値を空文字列に変更
2. **BulkRegisterTab 検索機能全面改修**
   - 会員番号欄プレースホルダー「番号/氏名」→「番号」
   - 氏名欄入力時→氏名で検索（所属設定時は氏名＋所属で絞り込み）
   - スペース正規化（半角・全角スペース除去して比較、DB側も同様）
   - 会員番号＋氏名入力時→両方一致必須（どちらか不一致→「該当者なし」赤表示）
3. **BulkRegisterTab「番号」列→「人数」列に変更**
   - 組・番号ではなく単純な連番（1,2,3...）を表示
   - 保存時に空きスロットを自動割り当て（組不足なら自動追加）
4. **共通プログレスインジケータ追加** — `components/LoadingOverlay.tsx` 新規作成
   - 画面中央オーバーレイ（金色スピナー）、全コンポーネントに適用
5. **閲覧者ログイン＆履歴機能追加**
   - `viewer_logs` テーブル追加（`db/migrations/003_create_viewer_logs.sql`）
   - API: `viewer-login`（POST, 氏名部分一致認証＋ログ記録）、`viewer-logs`（GET, 履歴取得）
   - `ViewerLoginForm.tsx` 新規作成（閲覧前ログインフォーム）
   - `ViewerPage.tsx` に未ログイン時のログインフォーム表示を追加
   - `ViewerHistoryTab.tsx` 新規作成（集計カード・所属フィルタ・30件ページング）
   - `AdminDetail.tsx` に「閲覧履歴」タブ追加（成績確認の右隣）
   - `lib/types.ts` に `ViewerLog` 型追加
   - `app/api/players/route.ts` に `q_name`/`q_belong` パラメータ追加（スペース正規化氏名検索）

---

### v1.8 — 選手一括登録タブ追加 + 列順変更（2026-03-31）
1. **BulkRegisterTab.tsx新規作成** — 選手一括登録コンポーネント
   - 初期24行（6名×4組）、＋追加で6行ずつ追加
   - 検索・設定ボタン: 会員番号（完全一致）または氏名（部分一致）でplayer_master検索
   - 複数ヒット時は同じ番号に複数行展開（ユーザーが×で削除選択）
   - 保存ボタン: 検索・設定後のみ有効、氏名空欄行はスキップ、重複番号はエラー
2. **MembersTab更新**:
   - 「選手一括登録」タブを1日目・2日目の左端に追加
   - 入力テーブルの列順変更: 番号|会員番号|氏名|**所属|クラス|審判フラグ**|削除

---

### v1.7 — 選手マスター機能追加（コミット: 予定）
1. **player_masterテーブル作成** — 会員番号・氏名・所属・クラス・審判フラグ・updated_at
2. **初期データインポート** — Excelファイル（近畿2025選手リスト）から159名をインポート
3. **API追加** — `/api/players` (GET/POST)、`/api/players/[code]` (PUT/PATCH/DELETE)
4. **選手マスター管理画面追加** — `/admin/players`（一覧・検索・登録・編集・削除）
5. **管理トップに選手マスターボタン追加** — `/admin` の大会一覧上部に配置
6. **MembersTab改善**:
   - 会員番号入力後にDBから氏名・所属・クラス・審判フラグを自動補完
   - 所属フィールドをテキスト→ドロップダウン（47都道府県＋芸文、デフォルト大阪）
   - 「この組を保存」時にplayer_masterのクラス・審判フラグをPATCHで自動更新
7. **lib/prefectures.ts追加** — PREFECTURES定数（47都道府県+芸文、コード順）

---

### v1.6 — UI改善・主催機能追加（コミット: 予定）
1. **AdminDetail**: タブ「設定」→「大会設定」、一番左に移動
2. **AdminDetail**: タブ行右端に「閲覧用確認 ↗」ボタン追加（別タブで閲覧画面）
3. **MembersTab**: 組替モードテキスト変更「⠿ アイコンをドラッグして移動」
4. **ViewerPage**: 🎯アイコン削除
5. **ViewerPage**: 「閲覧用QRコード表示」ボタン追加（QRCodeSVGで自動生成）
6. **tournaments**: `organizer_cd` カラム追加（INTEGER, DEFAULT 27=大阪）
7. **SettingsTab / admin/page.tsx**: 主催リストボックス追加（大会名の前、6府県）

---

### v1.5 — 成績確認・閲覧画面の表示改善（コミット: 5841c7e）
1. **審判フラグ⚑の位置変更** — 氏名の前→後ろ（`⚑ C君` → `C君 ⚑`）
   - 対象: ResultsTab.tsx、ViewerPage.tsx
2. **列ヘッダー変更** — `氏名` → `氏名　審判フラグ`
   - 対象: ResultsTab.tsx、ViewerPage.tsx

---

### v1.0 — 初期リリース（コミット: 2fad235）
**基本機能の実装**
- 大会管理（CRUD）
- 選手登録（組・日程別）
- 点数登録（R1〜R8）
- 成績確認（順位・合計・平均）
- 設定画面（大会情報・QRコード）
- Neon PostgreSQL連携
- Vercel デプロイ

---

### v1.1 — バグ修正・UI改善（コミット: 4667be2）
- ヘッダーアイコン（🎯）削除
- 日付入力をカレンダーピッカー（react-datepicker）に変更
- 会員番号：全角→半角自動変換
- 審判フラグ：「氏名」列に⚑マーク統合
- 点数入力フィールドを150%に拡大（44px→66px）
- バリデーションエラーを具体的に（組/氏名/ラウンド/値を明示）
- 設定画面にもカレンダーピッカー導入
- QRコードをファイルアップロード→自動生成（qrcode.react）に変更

---

### v1.2 — 重大バグ修正（コミット: c2e53fd）
**修正内容：**
- `r.average?.toFixed is not a function` エラー修正
  - 原因：PostgreSQLがNUMERIC型の集計値を文字列で返す
  - 修正：`r.average?.toFixed(2)` → `Number(r.average).toFixed(2)`
  - 対象ファイル：ResultsTab.tsx、ViewerPage.tsx

---

### v1.3 — 大規模UI改善（コミット: 4ee0cb3）
**13項目の同時実装**

1. **カラーテーマ更新**
   - C.muted: #7a8099 → #a8b4cc（明るく）
   - C.text: #e8eaf0 → #f0f2f8（明るく）

2. **トップページ全面再設計**
   - 管理者/閲覧者メニューを廃止
   - 大会一覧を直接表示
   - 隠し管理者ボタン（左下、黒、不可視）

3. **テキスト全体+2px拡大**

4. **射撃場名・日付テキストを白（#ffffff）に**

5. **テンキーポップアップ（ScoresTab）**
   - 従来：横移動する数値入力フィールド
   - 現在：クリックでポップアップ表示（0-9/C/決定）
   - 23〜25点：赤色表示
   - 「決定」で次の選手へ縦移動

6. **DnD選手組替（MembersTab）**
   - @dnd-kit/core, @dnd-kit/sortable 導入
   - ドラッグ&ドロップで組間移動可能
   - スロット固定、選手データが移動する設計

7. **選手削除機能**
   - 入力行：✕ボタンでクリア
   - 登録済み一覧：「削除」ボタン（点数ありは確認ダイアログ）
   - API: DELETE /api/tournaments/[id]/members/[memberId]

8. **空きスロットハイライト（B案採用）**
   - ゴールド左ボーダー + 「－（空き）」テキスト

9. **「メンバー」→「選手」全置換**

10. **閲覧者QR URL変更**
    - `/viewer/${tournamentId}` → `/viewer`（大会一覧ページ）

11. **QRコードにURLコピーボタン追加**

12. **react-datepicker でカレンダーアイコンクリック対応**

---

### v1.4 — 選手登録画面改善（コミット: 1af4560）
1. **重複エラーメッセージを具体化**
   - 旧：「同日程内に会員番号の重複があります」
   - 新：「2組 3番の会員番号「8888」と1組 1番の会員番号が重複しています」

2. **「選手組替」ボタンを右端→左端へ移動**

3. **「氏名　審判フラグ」列を分割**
   - 「氏名」列と「審判フラグ」列を独立した列に分離

4. **「削除」ヘッダーを赤字で追加**
   - ✕ボタン列のヘッダーを空白→「削除」（#e74c3c）に変更

---

## 却下・保留になった案

| 案 | 内容 | 理由 |
|----|------|------|
| 選手削除 A案 | 入力フォームのみクリア | 機能不足 |
| 選手削除 B案 | すべて確認ダイアログ付き削除 | 煩雑 |
| 選手削除 C案 | 入力クリア+DB削除（点数ありのみ確認） | **採用** |
| 空きスロット A案 | 薄いオーバーレイのみ | 見づらい |
| 空きスロット B案 | ゴールド左ボーダー+「－（空き）」テキスト | **採用** |

---

## 既知の問題・制約

| 問題 | 状況 | 対応 |
|------|------|------|
| WindowsローカルでEPERMエラー | `next build` 失敗 | Vercelでビルド（Linux）は正常 |
| PostgreSQL数値型→文字列返却 | NUMERICはJSでstring | `Number(x).toFixed()` で変換 |
| 会員番号なしの選手は点数登録不可 | scoresのmember_codeはNOT NULL | 仕様通り、UIで警告表示 |
